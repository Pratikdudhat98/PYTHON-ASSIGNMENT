# How can you pick a random item from a range?
     Use the random.randrange() function(Returns a random number within the specified range) to generate a random number within the 
given range by passing minimum, and maximum numbers as arguments.
    Generate a random number from 1 to 50 alternatively(step-2) by passing start, stop and step values as arguments to the randrange()
function.