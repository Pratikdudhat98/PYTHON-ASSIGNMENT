# How can you pick a random item from a list or tuple? 
We can use the random.choice()  function if we want to select a random item from a list, tuple, or string. This function accepts
 a sequential object like a list, string, and tuple as an argument and returns a random item or element from them.